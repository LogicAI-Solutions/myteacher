import requests

BASE_URL = "http://localhost:8000"

def test_student_creation_empty_username():
    # This assumes the server is running and we can authenticate or it's in dev mode
    # Since I don't have authentication tokens easily, I might not be able to run this against the actual server
    # if it's secured.
    # However, I can check the code logic.
    pass

if __name__ == "__main__":
    print("Verification script ready.")
